{
  "mcpServers": {
    "sequant": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT:-.}/scripts/mcp-launch.mjs", "sequant@2.15.1"],
      "env": { "SEQUANT_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}" }
    }
  }
}
